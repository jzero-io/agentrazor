# AgentRazor 文档

基于 VuePress 2 和 VuePress Theme Hope 的中英文文档站点。

## 本地开发

```bash
npm install
npm run dev
```

默认内容目录为 `src`，中文文档位于根路径，英文文档位于 `src/en`。

## 构建

```bash
npm run build
```

构建产物输出到 `src/.vuepress/dist`，该目录不提交到 Git。
