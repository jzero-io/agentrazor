# The Admin image builds from the repository root. Keep the context limited to
# the Admin application; plugin locales are loaded from Server at runtime.
*
!admin/
!admin/**

admin/node_modules
admin/dist
admin/.pnpm-store
**/.git
**/node_modules
**/dist
